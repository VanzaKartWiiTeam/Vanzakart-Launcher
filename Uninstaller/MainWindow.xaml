<Window x:Class="VanzaKartUninstaller.MainWindow"
        x:Name="UninstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="VanzaKart Uninstaller"
        Width="900"
        Height="690"
        MinWidth="900"
        MinHeight="690"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/Assets/app.ico"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        RenderOptions.BitmapScalingMode="HighQuality">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42"
                            ResizeBorderThickness="6"
                            GlassFrameThickness="0"
                            CornerRadius="16"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="TextPrimary" Color="#F7FAFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A8B4CE"/>
        <SolidColorBrush x:Key="TextFaint" Color="#687794"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#F012192A"/>
        <SolidColorBrush x:Key="InputBrush" Color="#0B1020"/>
        <SolidColorBrush x:Key="StrokeBrush" Color="#2A3857"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF6B82"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4DFFB0"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FFD166"/>

        <LinearGradientBrush x:Key="WindowGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Offset="0.00" Color="#070A13"/>
            <GradientStop Offset="0.38" Color="#0F1426"/>
            <GradientStop Offset="0.70" Color="#061622"/>
            <GradientStop Offset="1.00" Color="#140A1F"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="RainbowGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Offset="0.00" Color="#FF0066"/>
            <GradientStop Offset="0.18" Color="#FF8800"/>
            <GradientStop Offset="0.34" Color="#FFEA00"/>
            <GradientStop Offset="0.50" Color="#00FF66"/>
            <GradientStop Offset="0.67" Color="#00F2FF"/>
            <GradientStop Offset="0.84" Color="#3300FF"/>
            <GradientStop Offset="1.00" Color="#B000FF"/>
        </LinearGradientBrush>

        <DropShadowEffect x:Key="CardShadow" BlurRadius="30" ShadowDepth="0" Opacity="0.32" Color="#000000"/>
        <DropShadowEffect x:Key="NeonGlow" BlurRadius="28" ShadowDepth="0" Opacity="0.62" Color="#00F2FF"/>
        <DropShadowEffect x:Key="DangerGlow" BlurRadius="28" ShadowDepth="0" Opacity="0.65" Color="#FF0066"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="GlassCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource StrokeBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

        <Style x:Key="MetricCard" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Padding" Value="16"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect" Value="{x:Null}"/>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="#1A253D"/>
            <Setter Property="BorderBrush" Value="#344563"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="MinWidth" Value="128"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#223251"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#00F2FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2B3C61"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.03" Duration="0:0:0.14"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.03" Duration="0:0:0.14"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.14"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.14"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0.96" Duration="0:0:0.06"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.96" Duration="0:0:0.06"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.03" Duration="0:0:0.10"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.03" Duration="0:0:0.10"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="BorderThickness" Value="1.8"/>
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Offset="0" Color="#FF0066"/>
                        <GradientStop Offset="0.55" Color="#FF8800"/>
                        <GradientStop Offset="1" Color="#B000FF"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#2A0D1D"/>
            <Setter Property="Effect" Value="{StaticResource DangerGlow}"/>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#24304A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#314263"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.08" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.08" Duration="0:0:0.12"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.12"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0.92" Duration="0:0:0.06"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.92" Duration="0:0:0.06"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.08" Duration="0:0:0.10"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.08" Duration="0:0:0.10"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource RainbowGradient}"/>
            <Setter Property="Background" Value="#0A1020"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="7" ClipToBounds="True">
                            <Grid x:Name="PART_Track">
                                <Rectangle x:Name="PART_Indicator" HorizontalAlignment="Left" Fill="{TemplateBinding Foreground}" RadiusX="7" RadiusY="7"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="RootShell"
            Background="{StaticResource WindowGradient}"
            BorderBrush="#2D3A56"
            BorderThickness="1"
            CornerRadius="16"
            ClipToBounds="True">
        <Grid>
            <Border x:Name="AmbientOne" Width="360" Height="360" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-150,-120,0" CornerRadius="180" Opacity="0.16">
                <Border.Background>
                    <RadialGradientBrush>
                        <GradientStop Offset="0" Color="#88FF0066"/>
                        <GradientStop Offset="0.42" Color="#55FF8800"/>
                        <GradientStop Offset="1" Color="#00FFFFFF"/>
                    </RadialGradientBrush>
                </Border.Background>
            </Border>

            <Border x:Name="AmbientTwo" Width="300" Height="300" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-110,0,0,-140" CornerRadius="150" Opacity="0.12">
                <Border.Background>
                    <RadialGradientBrush>
                        <GradientStop Offset="0" Color="#9900F2FF"/>
                        <GradientStop Offset="0.42" Color="#663300FF"/>
                        <GradientStop Offset="1" Color="#00FFFFFF"/>
                    </RadialGradientBrush>
                </Border.Background>
            </Border>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="42"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="84"/>
                </Grid.RowDefinitions>

                <Grid x:Name="TitleBar" Grid.Row="0" Background="#70070B16" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0,0,0">
                        <Image Source="/Assets/logo.png" Width="24" Height="24" Stretch="Uniform"/>
                        <TextBlock Text="VANZAKART UNINSTALLER" FontSize="13" FontWeight="Black" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        <Border Background="#2A1020" BorderBrush="#7A2F45" BorderThickness="1" CornerRadius="5" Padding="9,3" Margin="14,0,0,0">
                            <TextBlock Text="Uninstaller" FontSize="11" Foreground="#FF9FB0"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Style="{StaticResource IconButton}" Content="&#xE921;" Click="MinimizeButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                        <Button Style="{StaticResource IconButton}" Content="&#xE8BB;" Click="CloseButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="24,18,24,0">
                    <Border Style="{StaticResource GlassCard}" Padding="22">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Uninstall VanzaKart" FontSize="26" FontWeight="Black"/>
                                    <TextBlock Text="The launcher will be fully removed." Foreground="{StaticResource TextSecondary}" FontSize="14" Margin="0,7,0,0"/>
                                </StackPanel>
                                <Image Grid.Column="1" Source="/Assets/logo.png" Width="72" Height="72"/>
                            </Grid>

                            <UniformGrid Grid.Row="1" Columns="3" Margin="0,18,0,16">
                                <Border Style="{StaticResource MetricCard}" Margin="0,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="VERSION" Foreground="{StaticResource TextFaint}" FontSize="11" FontWeight="Bold"/>
                                        <TextBlock x:Name="VersionTextBlock" Text="-" FontSize="18" FontWeight="Black" Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource MetricCard}" Margin="8,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="INSTALLED SIZE" Foreground="{StaticResource TextFaint}" FontSize="11" FontWeight="Bold"/>
                                        <TextBlock x:Name="SizeTextBlock" Text="-" FontSize="18" FontWeight="Black" Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource MetricCard}" Margin="8,0,0,0">
                                    <StackPanel>
                                        <TextBlock Text="LOCATION" Foreground="{StaticResource TextFaint}" FontSize="11" FontWeight="Bold"/>
                                        <TextBlock x:Name="PathTextBlock" Text="-" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.1*"/>
                                    <ColumnDefinition Width="18"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="Removal options" FontSize="16" FontWeight="Black" Margin="0,0,0,8"/>
                                    <Border Style="{StaticResource MetricCard}" Padding="16">
                                        <StackPanel>
                                            <CheckBox x:Name="DeleteModCheckBox" Content="Remove Stable and Beta modpacks" IsChecked="True" Style="{StaticResource ModernCheckBox}"/>
                                            <TextBlock Text="Removes both VanzaKart and VKBeta when they are installed." Foreground="{StaticResource TextFaint}" FontSize="12" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Style="{StaticResource MetricCard}" Margin="0,14,0,0" Padding="16">
                                        <StackPanel>
                                            <RadioButton x:Name="KeepUserDataRadioButton" Content="Keep user data" IsChecked="True" GroupName="UserDataMode" Style="{StaticResource ModernRadioButton}"/>
                                            <RadioButton x:Name="DeleteAllUserDataRadioButton" Content="Delete all user data" GroupName="UserDataMode" Style="{StaticResource ModernRadioButton}"/>
                                            <TextBlock Text="User data includes saves, profiles, preferences, and backups" Foreground="{StaticResource TextFaint}" FontSize="12" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Status" FontSize="16" FontWeight="Black" Margin="0,0,0,8"/>
                                    <TextBlock x:Name="StatusTextBlock" Text="Ready." Foreground="{StaticResource TextSecondary}" FontSize="14" TextWrapping="Wrap"/>
                                    <ProgressBar x:Name="UninstallProgressBar" Style="{StaticResource ModernProgressBar}" Minimum="0" Maximum="100" Margin="0,18,0,0" Visibility="Collapsed"/>
                                    <Expander Header="Uninstall log" Foreground="{StaticResource TextPrimary}" IsExpanded="True" Margin="0,20,0,0">
                                        <Border Background="#990B1020" BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,10,0,0" Height="160">
                                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                <TextBlock x:Name="LogTextBlock" Text="Log initialized." FontFamily="Consolas" FontSize="12" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Expander>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="24,12,24,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="CancelButton" Content="Cancel" Style="{StaticResource ModernButton}" Click="CancelButton_Click"/>
                    <TextBlock x:Name="FooterStatusTextBlock" Grid.Column="1" Text="Ready to uninstall." Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" TextAlignment="Center"/>
                    <Button x:Name="UninstallButton" Grid.Column="2" Content="Uninstall" Style="{StaticResource DangerButton}" Click="UninstallButton_OnClick"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
